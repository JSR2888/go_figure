/* =========================================================
   PUZZLE_BANK — this is the file you'll edit most often.

   One entry per day. Each entry can have as many or as few
   digits as you like (2, 4, 7, whatever makes a good puzzle);
   duplicates are fine too (each becomes its own tile, tracked
   independently).

   Key format: 'YYYY-MM-DD' in UTC (so the puzzle changes at
   UTC midnight, the same moment for everyone).

   Optional: before adding a puzzle, sanity-check it's actually
   solvable by opening the browser console on the live site and
   running GoFigure.isSolvable([7, 9, 7, 9]) -> true/false.
   It only tells you a solution exists, not whether it's a good
   puzzle (7,9,7,9 is technically solvable and still dull) — use
   it to catch dead ends, not to pick the puzzle for you.
   ========================================================= */

const PUZZLE_BANK = {
  '2026-07-08': [1, 2, 3, 4],
  '2026-07-09': [2, 3, 4, 8],
  '2026-07-10': [1, 3, 4, 6, 9],
  // Add tomorrow's date and digits here whenever you're ready.
};
